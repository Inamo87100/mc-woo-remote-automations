<?xml version="1.0"?>
<ruleset name="MC-Woo Remote Automations">
    <description>WordPress Coding Standards configuration for MC-Woo Remote Automations</description>

    <!-- What to scan -->
    <file>mc-woo-remote-automations.php</file>
    <file>includes/</file>
    <file>admin/</file>

    <!-- Exclude build and vendor files -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <!-- Use WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Allow short ternary operator -->
        <exclude name="Universal.Operators.DisallowShortTernary"/>
    </rule>

    <!-- Minimum PHP version -->
    <config name="minimum_supported_wp_version" value="5.0"/>
    <config name="testVersion" value="7.4-"/>

    <!-- WordPress-specific security rules -->
    <rule ref="WordPress.Security.EscapeOutput"/>
    <rule ref="WordPress.Security.NonceVerification"/>
    <rule ref="WordPress.Security.ValidatedSanitizedInput"/>
    <rule ref="WordPress.DB.PreparedSQL"/>
    <rule ref="WordPress.DB.PreparedSQLPlaceholders"/>

    <!-- Text domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array" value="mc-woo-remote-automations"/>
        </properties>
    </rule>

    <!-- Use tabs for indentation -->
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent"/>
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="4"/>
            <property name="tabIndent" value="true"/>
        </properties>
    </rule>

    <!-- File naming -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="true"/>
        </properties>
    </rule>
</ruleset>
